1. Package management:
    - ONLY use `uv`, NEVER use `pip` directly

2. Code style:
    - use `ruff` for formatting and linting according to the pre-commit configuration

3. Project structure:
    - this project follows the cookiecutter template from https://github.com/astroufsc/chimera-template and should maintain compatibility with future updates to that template. Use `cruft` to manage template updates.